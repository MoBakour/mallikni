import { IProperty } from "../../types/types";

interface IDetailsTab {
    property: IProperty;
}

const DetailsTab = ({ property }: IDetailsTab) => {
    return <div>DetailsTab</div>;
};

export default DetailsTab;
