import { IProperty } from "../../types/types";

interface ILocationTab {
    property: IProperty;
}

const LocationTab = ({ property }: ILocationTab) => {
    return <div>LocationTab</div>;
};

export default LocationTab;
